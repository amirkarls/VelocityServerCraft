bind = "0.0.0.0:25565"
motd = "<#ff6600>🐉 Dragons Power Network"
show-max-players = 500
online-mode = false

player-info-forwarding-mode = "modern"
forwarding-secret-file = "forwarding.secret"

[servers]
# Сервер авторизации (капча + регистрация)
auth = "dragonpowers-hub.falix.gg:25565"

# Хаб-сервер (с NPC для выбора серверов)
hub = "dragonpowers-hub.falix.gg:25565"

# Гриф-сервер
grief = "dragon-powers.falix.gg:26563"

# Сервер по умолчанию — сначала авторизация
try = ["auth"]

[forced-hosts]
"localhost" = ["hub"]
