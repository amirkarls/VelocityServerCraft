bind = "0.0.0.0:25565"
motd = "<#ff6600>🐉 Dragons Power Network"
show-max-players = 500
online-mode = false

# Для LimboAuth нужен modern режим
player-info-forwarding-mode = "modern"
forwarding-secret-file = "forwarding.secret"

[servers]
# Виртуальный сервер для авторизации (создаётся LimboAPI)
auth = "localhost"

# Твой реальный хаб
hub = "dragonpowers-hub.falix.gg:25565"

# Гриф
grief = "dragon-powers.falix.gg:26563"

# Порядок: сначала авторизация
try = ["auth"]
